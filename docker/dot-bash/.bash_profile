#####################################################################
#
# I don't know exactly what this file does, but it's necessary to set
# bash .env into Alpine Linux container
#
#####################################################################

[[ -f ~/.bashrc ]] && . ~/.bashrc
